# Wenruo-RAG (文若 RAG) Instructions

Use this file as the local operating guide for the current codebase.
Prefer the current code and explicit architecture decisions in this file
and the current CLAUDE.md over older conventions or remembered project shape.

## Core Stance
- Treat legacy code as liability, not as a compatibility target.
- Prefer deletion over shims, deprecated branches, wrapper APIs, and dual-track migration notes.
- If old and new implementations coexist, converge to one path unless an external contract forces compatibility.
- Remove dead tests, commented-out code, stale docs, and "move later" notes instead of preserving them.
- Reduce public surface area when a helper can be made private or internal.
- Keep refactors centered on the owning abstraction, not on adjacent compatibility layers.

## Current Architecture Decisions (CRITICAL)
- **Active Backend**: `API_PROXY_SCHEME=python` is frozen as the sole active and supported backend path.
- **Python Authoritative**: Python (`api/`) is the single source of truth for current permission, RBAC, and tenant refactoring.
- **Go Backend Deprecation**: Go (`internal/`) permission and authentication paths are INACTIVE for current features.
- **No Dual Sync**: Do NOT attempt to revive or edit Go backend code for symmetry.
- **Error Response Standard**: Permission denials MUST follow RAGFlow standard `HTTP 200` with `code=108` (`get_error_data_result`) for seamless frontend Toast and error handling.
- **Frontend HTTP Payload Contract**: `umi-request` calls MUST wrap payload bodies in `{ data: body }` (e.g., `request.put(url, { data: { role } })`), otherwise parameters fail validation with `code=101`.

## Tenant & RBAC Model (CRITICAL)

The tenant role hierarchy is strictly enforced as:

**OWNER > ADMIN > NORMAL**

### Role Definitions
- **OWNER**: Tenant creator/owner. Full tenant authority, manages administrators and overall tenant settings.
- **ADMIN**: Granted tenant-management permissions by RBAC layer. Manages members and assigned configurations; cannot override OWNER.
- **NORMAL**: Standard tenant user. Cannot manage members, tenant-level providers, or system settings.

### Tenant Context & Multi-Tenancy Architecture
- **Active Tenant Context**: NEVER assume `user.id == tenant_id` for non-owner users. All workspace operations MUST be scoped to the `active_tenant_id` resolved via `TenantService.resolve_config_tenant_id` or active session/request context.
- **Joined Tenant Access**: NORMAL members without personal tenants inherit read-only access to their joined tenant's shared model configurations and knowledge bases (`resolve_config_tenant_id`).
- **Backend Enforcement**: Frontend UI/component hiding is NEVER an authorization boundary. Sensitive endpoints MUST enforce `@require_tenant_admin`.
- **Visibility vs Ownership**: `visibility` (e.g. `me` vs `team`) DOES NOT equal ownership.
- **Access vs Management**: Read/query permission DOES NOT imply permission to modify, delete, transfer, or manage.
- **Decoupled Roles & Attributes**: Tenant roles (`role` -> authorization) and organizational attributes (`department_id`, `title` -> profile) MUST be handled via distinct endpoints (`PUT /role` vs `PUT /profile`).
- **Private Visibility Semantics**: `private` (`permission='me'`) knowledge bases are visible to the **Creator + Tenant Owner/Admin** for enterprise governance and auditing.

## Permission Refactor Progress Base (P1 ~ P5)

Preserve and build upon these established milestones:

### Completed Milestones
- **The False Empty Answer (a pre-retrieval flag killed a full pool)**: A reasoning turn whose retrieval had finally worked still answered the configured fallback, and the transcript said why in two consecutive lines: `[Composing the answer] Writing the final answer from 8 gathered passage(s).` followed by `[Composing the answer] No supporting evidence was found; returning the configured empty response without calling the answer model.` The passages were there - the kill was `state["empty_result"]`, which `build_agentic_graph`'s `formalize_question` seeds as `True` BEFORE any retrieval runs, and which NOTHING ever cleared: `harness/orchestrator/direct.py::direct_search` wrote `True` again when it found nothing and wrote nothing at all when it found something, so the only value the key ever held was `True`. `_compose_answer_from_evidence` OR-ed it into its evidence test (`abstain or empty_result or not kbinfos["chunks"]`), so EVERY agentic (medium/high/ultra) turn abstained regardless of what research had gathered - the last gate in the chain, after retrieval, sub-queries, slots and SCA had all succeeded. It was masked until now because retrieval returned nothing anyway, so the canned answer looked correct. Fix, in one direction: the composer decides from the EVIDENCE it holds (`abstain or not kbinfos["chunks"]`), and the flag is DELETED rather than repaired - `AgenticState.empty_result` is gone from the typed state, from the agentic seed dict, from `direct_search`'s return and from the composer, because a flag that can only ever be written `True` tells a reader of the pool nothing the pool does not already say. `abstain` is kept: it is the one explicit post-retrieval verdict allowed to refuse a non-empty pool (and it too is never written `True` today, so it is inert-but-safe rather than a lie). Two diagnostics were added where the failure had none: the empty branch now names WHICH condition emptied the answer (`retrieval gathered no passage` vs `the verdict was to abstain`), and the composer WARNs when passages reached it yet `kb_prompt` rendered no evidence block inside the token budget - the same false "no evidence" one layer below the pool test, where `kb_prompt` silently drops a block that does not fit. Note the chain itself was verified intact, not assumed: research, SCA and the composer share one pool (`tools.kbinfos` / `state["kbinfos"]`), the SCA's review view never replaces it (its `kbinfos = {}` branch is a defensive fallback for a non-dict pool, with an ERROR log, and it returns no `kbinfos`), and `cite_chunks` can never be empty while `all_chunks` is not. 7 cases in `test/unit_test/rag/advanced_rag/test_compose_answer_evidence.py`: eight gathered passages reach the answer model even with the stale key present in the state (the exact reported failure), the prompt carries them, a genuinely empty pool still returns the configured fallback with the reason logged, an explicit abstain still wins over a non-empty pool, `direct_search` seeds no pre-retrieval flag, and the typed state no longer declares one.
- **Chat / Search / Agent Are Personally Private (the workspace-wide listing reverted)**: The previous fix bound applications to the workspace and authorized them by MEMBERSHIP, which made a workspace's chat and search lists team-wide - a member saw the owner's 架空绝缘导线助手, and my own AGENTS.md entry recorded that as the intent. It is not. A workspace shares its MODELS and its DATASETS, never the applications built on them, so ownership is the CREATOR alone while model/token consumption stays with the active workspace. `dialog` had no creator column at all (`tenant_id` was its only ownership, and that column now names the workspace), so `created_by` was added by an append-only migration and BACKFILLED as `created_by = tenant_id`: exact for rows created before workspace binding (tenant WAS the creator's user id), the workspace owner for the short window since. `migrate_dialog_created_by()` is idempotent and logged `Backfilled dialog.created_by for 17 assistant(s)` on the live stack. One rule now: `chat_api._accessible_chat` is `chat.created_by == current_user.id` with no membership branch; `GET /chats` passes the new `created_by` filter into `DialogService.get_by_tenant_ids` (plus the active workspace); `DELETE /chats {delete_all}` filters the same way; and the legacy "DELETE with chat_id in the body" path - which had NO guard and could soft-delete anyone's assistant from an id alone - goes through the same helper. Search mirrors it: `SearchService.get_by_tenant_ids` gained `created_by`, `POST /searches` binds `tenant_id = active_workspace_id` (a member's user id is not a tenant, which is what answered "Authorized identity.") with `created_by = current_user` and a per-creator duplicate check, detail/update/delete/completion go through `accessible4deletion`/`_owned_search` and answer 108, and the completion resolves chat/rerank models in the app's own workspace while the dataset read gate stays the caller's. Agents: `UserCanvasService._visible_to` - one predicate now shared by the list, both filter queries and the tag counts - requires `release` for the team branch, so an UNPUBLISHED agent is its creator's alone, and `accessible` follows (`release` added to `get_by_canvas_id`'s projection). Verified live with two accounts: the owner creates Chat A; as the member, `GET /chats` does not list A and `GET`/`PATCH`/`POST /chat/completions` on A are 108, while creating B and using it (settings, llm_id, session, a real answer) are all 0; back as the owner the list has A and not B, and B is 108. Search apps behave identically (the owner's app is invisible and 108 to the member; the member's own app lists, reads and runs). 16 + 6 + 6 cases in `test_chat_workspace_resolution.py` (rewritten), `test_search_personal_isolation.py` and `test_agent_visibility_isolation.py`; `test_session_dataset_binding.py` follows the new guard; the legacy `test_update_search_missing.py` stub was completed for the names the module now imports, with its "verified then deleted" case still answering DATA_ERROR. NOT addressed: an assistant whose `tenant_id` is not a tenant (the member's legacy `Test`, tenant = its own user id) is now owned and listed correctly but still cannot resolve a chat model, since that tenant does not exist; `update_message_feedback` still indexes chunk feedback by the caller's user id instead of the dataset's workspace; and a chat assistant still carries no rerank model unless an operator picks one.
- **Table Dominance in the Context Window (prose-tier route + type diversity floor)**: Four live smoke tests on the cable corpus separated what works from what does not. A question carrying the standard number (Q/GDW 73237.1) hit eleven prose passages of 《第1部分：通用技术规范》 and answered exactly (3.4/2.5mm, the 90% rule); the same question without it - and any question about 例行试验/优先权/维持时间 - came back with the 专用技术规范's bidder fill-in tables filling the window ("[1][5][8] 全是表格"), so the answer layer reported 只有表格，没有正文规定, or 查不到 for the 4.1.10 优先权 clause. The tables are not noise: they repeat every parameter name and unit the question uses, so they win the fused score, and a plain top-N then hands over nothing else. Three changes, all in `rag/retrieval/`. (1) `chunk_profile.py` reads what a passage IS - `doc_type_kwd` (the parsers' own label: `table`/`image`, copied onto every retrieved passage by `Dealer.retrieval`), HTML table markup, an image with no text - and which DOCUMENT it came from. `row_id` is deliberately NOT a table signal: it is a doc-store row identity, and Infinity answers `row_id()` for every row, so treating it as "table row" would relabel the whole corpus. (2) `decomposition.clause_route` is a deterministic LLM-free extra route for a rule question (cues: 例行…试验/型式…试验/试验标准|方法|条件|项目、检验规则、规则/条款/条文/规定/优先/为准/矛盾/不一致/判定/维持时间/时限…): the user's own subject with section references stripped, plus the anchor `通用技术规范 正文条款`. The anchor is a semantic tier name that sits in the prose document's TITLE, and doc-store title tokens are boosted ^10/^5, so it RAISES the passages it is meant to find instead of diluting them the way an arbitrary appended keyword does (the failure recorded in the milestone below). It fires on every clause question, including the ones the LLM node failed on or never saw, and it is not derived from the literal 例行试验: the live wording was 例行**交流电压**试验, which a term list without a gap misses. (3) `rerank.DiversityPolicy` + `select_context` add a TYPE floor to the cut for a rule question - at least `MIN_PROSE_PASSAGES` (4) non-table passages, tables capped at `MAX_TABLE_SHARE` (0.5) of the window, and a 0.85 ordering nudge for tables. A question that merely mentions a test or a requirement (绝缘电阻试验的数值是多少) gets the nudge only, because a parameter table IS its right source; a value question (绝缘标称厚度是多少) gets a plain top-N, so the Test 2 shape that works today is untouched. Two limits are stated rather than hidden, both in the transcript: a cap the pool cannot fill returns a SHORTER window instead of trading the withheld slots for more tables (twelve tables + three clauses at top_n=12 -> 9 passages, logged with the pool's composition), and a pool holding NO prose cannot have its floor met - that is a RECALL failure and the stage says so in a WARNING, so the next smoke test can tell "the cut dropped the clause" from "the clause never came back". The applied penalty is on the merged pool's ORDER, not inside Dealer's per-route scoring, so it cannot recover a passage a route's own window already dropped - which is exactly why the prose-tier route and the warning exist. `ensure_route_coverage` remains the inert special case of `select_context`, so the route-coverage behaviour and its 7 cases are unchanged. 23 cases in `test/unit_test/rag/retrieval/test_context_diversity.py` plus 2 at the harness seam (`test_hybrid_search_multi_route.py`, now 9) covering what the Web UI runs: a clause question searches the prose tier, and the clause beats the table it lost to. NOT addressed: the DOCUMENT-share cap (the alternative wording of the same request) was deliberately not taken, because capping one document at half the window would have cut the winning Test 2 answer from 11 passages to 6; and the ordering change is a nudge, not a filter, so a table that is genuinely the best match for a rule question can still lead the window.
- **Non-Owner Chat Permissions (109 -> the workspace model)**: The tenant administrator `0realannabelle0@gmail.com` got `109 no authorization` on 【新建聊天】/chat settings. Reproduced against the running stack: the member has NO tenant of its own (its single `user_tenant` row is the owner's workspace, role=admin), but `POST /chats` stored `tenant_id = current_user.id` - a user id that is not a tenant. Two different ownership notions then disagreed: `_ensure_owned_chat` read `tenant_id == current_user.id` (so create/PATCH/DELETE passed) while `GET /chats/<id>` looked the assistant up under the caller's `user_tenant` workspaces (so it matched nothing and answered 109). Measured as the member: `GET /chats/<id>` -> **109**, `PATCH /chats/<id>` with `llm_id` -> **102** "`llm_id` … doesn't exist" (the model was resolved in "tenant = member user id", which owns no models), `POST /chat/title` -> **101** "No chat model is available…", and any turn would have failed the same way because `get_models` resolves the chat/rerank/TTS models in `dialog.tenant_id`. Fix: an assistant is bound to the WORKSPACE it is used in (`create` writes `tenant.id`), and one helper `chat_api._accessible_chat` authorizes as "CREATOR or MEMBER of the owning workspace" - both halves are needed, the creator half for a personal workspace (which IS the user id) and for every assistant created before this change, the membership half for an admin/member. Every chat/session/completion/title/audio/mindmap route now takes its model subject from the active workspace (or the assistant's own tenant), a genuine refusal is **code 108** (109 means "not authenticated"), and `GET /chats` + `DELETE /chats {delete_all}` are scoped to the active workspace plus the caller's own id. Verified against the live stack as the member: GET /models 75 models, POST /chats -> 0 (row bound to the workspace), GET /chats/<id> -> 0, GET /chats lists it, PATCH name/prompt_config/llm_id/dataset_ids -> 0, POST sessions -> 0, POST /chat/title -> 0 (title 绝缘标称厚度), POST /chat/completions -> 0 with a real answer. `test_chat_workspace_resolution.py` was rewritten to pin the new design (12 cases: workspace binding, model subject, creator branch, membership branch, 108 for a stranger, soft-deleted, list scope); `test_session_dataset_binding.py` follows the renamed guard. NOT addressed: `search_api.py::detail/update/delete/completion` (the same "iterate my memberships" pattern, so a member's own search app answers 109) and `Search.tenant_id = current_user.id` in `POST /searches`; one pre-existing orphan assistant (`Test`, `tenant_id` = the member's user id) is readable and listed now but still cannot resolve a chat model, because its tenant does not exist - deleting it or a one-off rebind is a data decision, not a code one; `update_message_feedback` still passes `current_user.id` to `ChunkFeedbackService.apply_feedback`, which indexes by that id, so chunk feedback wants the DATASET's tenant instead.
- **Multi-Route Retrieval for Composite Questions**: A question asking for several parameters at once ("普通绝缘和轻型薄绝缘的厚度、绝缘电阻和电压试验有什么区别？") was answered 暂未在知识库中检索到相关线缆参数或规范条款 while its own corpus held 5.3.3 (绝缘标称厚度) and 6.2 (例行交流电压试验). One `Dealer.retrieval` call with one query string is why: the text leg is a query-RECALL ratio and the vector leg is one embedding, so each chapter's clause scores a fraction of what it scores on its own sub-question and the pool is emptied at the assistant's 0.55 gate. `api/db/services/dialog_service.py::async_chat` now calls `rag/retrieval/pipeline.py::retrieve_multi_route`, and the agentic harness was moved onto the same pipeline as well - that second half is what made the feature reachable in the Web UI, whose Naive/low selection answers `[Agentic RAG] mode=low`, so retrieval ran `build_low_graph` -> `direct_search` -> `hybrid_search` (see the `hybrid_search` paragraph at the end of this entry). Module A (`rag/retrieval/decomposition.py`) splits a composite question into atomic sub-queries behind a cheap deterministic gate (`looks_composite`), so a single-parameter question still costs ONE retrieval and NO LLM call, and strips structural references (`第5章`, `5.3.3`, `附录A`) out of a search statement while leaving numeric data (`0.6/1kV`, `1.2mm`, `20个工作日`) verbatim. Module B (`rag/retrieval/multi_route.py`) retrieves the original question plus every sub-query concurrently - hybrid per route, 12 passages per route (the recommended 10-15 band), dense 0.6 : sparse 0.4 by default with the assistant's configured weight winning - merges by `chunk_id` keeping the best score each passage earned and recording `retrieval_routes`/`route_hits`, sums `doc_aggs` per document, isolates a failing route, and retries a route the configured threshold emptied at the 0.2 recall floor (the same rescue as the agentic harness). Module C (`rag/retrieval/rerank.py`) dedupes, scores the union with the reranker against the user's ORIGINAL question, REPLACES the fused score with the reranker's own score instead of averaging it with a recall ratio that penalizes every dimension a passage does not cover (`fused_similarity` keeps the old value), and cuts to `top_n` (default 8, inside the recommended 6-8; a configured `top_n` wins, so the cable assistant keeps 12). The CUT is not a plain top-N: one slot per route is reserved before the remaining slots take the highest scoring passages (`ensure_route_coverage`). Measured live on the cable dataset, that is the difference between the feature working and not: with the five-parameter question 同时对比绝缘标称厚度、绝缘电阻、交流电压试验、外护套材料、铠装类型的区别？, the routes merged 35 passages (including `[标准号: Q/GDW 73237.1-2026 | 章节: 6.2.3 交流电压试验]` at 0.577), and the plain top-12 kept TWELVE near-identical 技术参数特性表 fragments of the leading dimension (0.596-0.603) - the answer then reported 交流电压试验/绝缘电阻/外护套/铠装 as 暂未包含 while holding the clause it needed; with the coverage pass the same 12 slots carry the 6.2.3 clause and each route's best passage, and the same turn now answers 18 kV/12 kV 1 min from 6.2.3 (plus the 4 h sampling test) and says honestly which dimensions the corpus does not cover. Note the assistant had NO reranker configured (`dialog.rerank_id` empty), so this was the fused-score fallback - a chat assistant does not get one unless an operator picks it. Routes no longer rerank individually: one pass over the union. Every failure path degrades - no chat model, a failed decomposition, a dead route, a missing or failing reranker all fall back rather than emptying the answer. 77 cases in `test/unit_test/rag/retrieval/`; the composite case drives a doc-store double whose score IS the query-recall ratio, reproduces the 暂未查到 symptom, and asserts chapter 5 AND chapter 6 reach the final context - including a flood corpus that reproduces the twelve-table monopoly. The agentic harness now runs the same pipeline instead of its own single-query leg: `rag/advanced_rag/harness/tools/search.py::hybrid_search` calls `retrieve_multi_route`, and that IS the seam the product reaches - the Web UI's Naive/low selection logs `[Agentic RAG] mode=low`, builds `build_low_graph`, and its `direct_search` node calls `hybrid_search` with the user's question. So low/naive mode and every `search_chunks` / `metadata_search` / fan-out channel now decompose a composite question; a caller's `top_n` is both the window each route gets and the number of passages the search returns (a route that cannot fill the page it has to return cannot contribute to it); the empty-pool rescue lives only in `multi_route.RECALL_FLOOR` - the harness-local `_THRESHOLD_RESCUE_FLOOR` is deleted; the harness deliberately attaches no rerank model (`RAGTools` carries none, and one harness question issues many searches, each rerank a provider round trip), so it takes module C's fused-order fallback; `bm25_search` and `grep_search` stay single-leg by definition, and `RAGTools.retrieve` (reachable only through `_naive_rag`, i.e. on an unrecognised mode label) still sends one query. Test harness: `test_gaussdb_dialog_sql.py` (36 cases driving the real `async_chat`) could not be COLLECTED on this checkout before this work - its `tenant_model_service` / `common.token_utils` / `rag.prompts.generator` stubs predated names the real modules import (`get_default_rerank_model_config`, `total_token_count_from_response`, `truncate`, `vision_llm_describe_prompt`); the stubs are completed, its three `assert_awaited_once_with` retrieval contracts follow the pipeline, and the file passes 36/36 (verified pre-existing: restored to HEAD it fails the same way). `test_search_retrieval_settings.py`'s rescue assertions now follow `multi_route.RECALL_FLOOR`, and `test_hybrid_search_multi_route.py` (7 cases) drives the harness seam - decomposition fires once, chapter 5 AND chapter 6 reach the search result, an atomic query still costs one retrieval and no LLM call, `top_n` still caps what the search returns. NOT addressed: `async_ask` (search app) keeps its own retrieval path; `cable_defaults.SIMILARITY_THRESHOLD=0.55` is still an absolute gate calibrated on one corpus; and when a tenant enables `prompt_config["keyword"]`, `async_chat` still concatenates the extracted keywords onto the scored query - the exact dilution fixed in the harness milestone below.
- **Agentic RAG Retrieval Recall (query scoring + threshold gate)**: 架空绝缘导线助手 answered 暂未在知识库中检索到相关线缆参数或规范条款 for questions its own dataset answers verbatim; the transcript showed `mode=low ... [Hybrid search] 0 chunk(s) (threshold=0.55, vector_weight=0.5)`. Two independent causes, both in `rag/advanced_rag/harness/tools/search.py::hybrid_search`. (1) The extracted keyword/entity expansion was CONCATENATED onto the scored query, but the fused score's text leg is a query-RECALL ratio (`Qryr.token_similarity` divides the matched term weight by the query's TOTAL term weight), so every appended term a passage does not contain LOWERS that passage's score: the answer chunk (5.8.11 "A、B、C类检测任务应分别在收样后20、15、10个工作日内完成") scored 0.6085 on its question alone and 0.5283 with the weighted expansion appended - same chunk, same rank 1, pushed under the threshold. Keywords are now a narrowing hint only; the dead `retrieval_query` parameter is deleted from `hybrid_search` and from `direct_search`, which also drops a duplicate keyword-extraction LLM call. (2) An absolute threshold over `vector_weight * cosine + term_weight * term_recall` is comparable only inside ONE corpus/embedding pair - on this dataset the embedding leg alone gives EVERY chunk ~0.45 (measured cosines 0.88-0.92), so 0.55 sat above the whole pool (top candidate 0.53-0.61). When the configured threshold returns nothing the search now re-runs ONCE at `_THRESHOLD_RESCUE_FLOOR` (0.2) and logs a WARNING naming both thresholds; a threshold that DOES discriminate keeps its full effect on the tail. The result cache key now includes `keywords`, because the cached entry is the NARROWED pool. `[Formalize]`'s single-turn line now states that the question is kept verbatim while keywords are still extracted: the old "(no rewrite)" wording read as if query preprocessing had been skipped, and it never was - the very same turn's `[Keywords]` call had already produced 检测机构 / A类 / B类 / C类 / 检测时限 / 工作日. Verified against the live dataset `557849ee...` (gemini-embedding-001) by overlaying the patched file on the running container and restoring it afterwards: threshold 0.55 -> 2 chunks with the answer span at rank 1, threshold 0.9 -> rescue fires -> 12 chunks, answer still rank 1. Seven new cases in `test/unit_test/rag/advanced_rag/test_search_retrieval_settings.py`; `advanced_rag` files, `rag/nlp` and the retrieval suites pass; `ruff` adds no finding. NOT addressed - needs a decision, not a bug left behind: `similarity_threshold=0.55` in `api/db/cable_defaults.py` is still an absolute gate calibrated on one corpus, and the scored query is still the natural-language question rather than a rewrite.
- **Shared Modal / Select Portal Protection**: With explicit shared-UI authorization, `Modal` now prevents outside-interaction dismissal for targets inside Radix Select viewports/listboxes. Removed its independent overlay-click and window-Escape close paths so Radix owns dismissal and Escape closes only the top layer. Nine targeted tests cover role/department selection, portalled options, genuine backdrop clicks, `maskClosable`, and nested Escape. Owner-session browser verification remains pending an authenticated session; unit tests are not a substitute for hit-testing in a browser.
- **Dataset Sidebar Compact Layout & i18n**: Dataset detail sidebar uses a two-row header: a truncated name beside the toggle, then a 32px identity icon beside compact file metadata. Menu rows are 36px with 4px gaps; a full-height flex column gives navigation its own scroll area and 16px bottom clearance. Artifacts uses the `knowledgeDetails.artifacts` translation key in Chinese and English. Collapse persistence and icon tooltips are preserved; shared UI and Chat modules are unchanged.
- **Dataset Sidebar & Personal List Sync**: Dataset detail sidebar collapses to a 64px icon rail with menu tooltips, a 200ms width transition, and `dataset_sidebar_collapsed` localStorage persistence. Home and Dataset lists share the SQL-authorized dataset listing, with user/workspace-scoped query keys and no previous-workspace placeholder rows. Personal preferences apply before pagination: hidden datasets are omitted on Home, pins sort first then `update_time` descending. Hiding cancels a pin; pinning restores a hidden dataset; persisted overlapping pins are migrated away. Pins never expand access or affect RAG retrieval weights. Chat/Conversation modules are unchanged.
- **P1-01**: Disabled public registration (`REGISTER_ENABLED=0`).
- **P1-02**: Role hierarchy definition & `can_manage_tenant` helper.
- **P1-03**: Backend decorator `@require_tenant_admin`.
- **P1-04**: System account `admin@ragflow.io` hardened, `ADMIN_DEFAULT_PASSWORD` neutralized in `docker/.env`, and test Normal user `0realannabelle0@gmail.com` initialized for E2E validation.
- **P1-05**: User API `/users/me` returning active `role` and resolved tenant scope.
- **P3-02**: Tenant member role update API (`PUT /tenants/<id>/users/<user_id>/role`), umi-request `{ data }` payload wrapper bug fix, and transport-layer service unit tests.
- **P4-00a/b/c**: Markdown & LaTeX rendering pipeline overhaul (fixed `\[...\]` block math line breaks, fixed `\(a\)` letter-ending inline math, added real-pipeline Sanitizer & KaTeX tests, Jest ESM transformer fixes).
- **P4-01**: Provider API Key output masking.
- **P4-02**: Comprehensive RBAC endpoint guards (`@require_tenant_admin` on 15 sensitive write/execute provider, model, and langfuse APIs; 8 read endpoints remain open).
- **P4-03a**: Standardized frontend `RoleTag` component with semantic tokens (`src/tailwind.css`), i18n support, and UI mounting.
- **P4-03b**: Frontend Model Settings read-only mode for NORMAL users (disabled controls, hidden action buttons, admin managed notice banner).
- **P4-05**: Multi-tenant model config resolution (`TenantService.resolve_config_tenant_id`) enabling NORMAL users to seamlessly read joined tenant model configurations without 102 Tenant errors.
- **P5-00**: Department & Title architecture (`department` table + `user_tenant.department_id`/`title` append-only columns), department CRUD API with non-empty member guards (`code=102`), profile update API (`PUT /profile`), and flat UI department management.
- **P5-01**: Global Active Tenant Context Middleware & `/user-setting/team` workspace switcher refactor.
- **Data Source Module Cleanup**: Migrated shared data-source modules from deprecated `pages/user-setting/data-source/` to `src/components/data-source/`, fixing relative imports across 11 files and completely deleting the user-setting data-source page.
- **P3-03 (Step 1 ~ 7 Complete)**: Full fine-grained dataset authorization rollout: `KnowledgebaseAuthorization` DB model, SQL-level dataset masking (`accessible`), split API endpoint for authorization (`PUT /datasets/<id>/authorization`), ECP zebra-striped table UI, department/user visibility selector, dataset pin/hide quick controls, and single-file hiding status synchronization with non-technical clean Tooltips (`隐藏文件`/`恢复显示`).
- **Phase 1 Workspace & Permission Sweep**: Complete resolution of NORMAL user errors (101/102/500 -> clean 108) across `/chats`, `/datasets`, `/mcp/servers`, and `/system/tokens` in joined workspaces; auto-injected `X-Tenant-Id` header in frontend umi-request layer; `/user-setting/team` roles and invitation fix. Every read endpoint was re-probed as `0realannabelle0@gmail.com` against the running stack: all `code=0`, every refusal a clean `108`, no `101`/`102`/HTTP 500. `POST /datasets` now creates in the workspace the caller works in with `created_by` set to the AUTHOR (a member used to get `101 "Unable to serialize unknown type: Response"` and could not see what it created); the retrieval and document paths take the model configuration and index from the DATASET's workspace; model defaults, provider instances and Langfuse keys are written into the workspace being edited, matching what the reads resolve; `GET /tenants` reports each workspace's own `name` instead of its owner's nickname.
- **Tenant-Admin Guard Subject**: `@require_tenant_admin` resolves the ACTIVE workspace (caller + validated `X-Tenant-Id`) and no longer reads `kwargs["tenant_id"]`, which `add_tenant_id_to_kwargs` fills with the caller's USER id. The old subject evaluated `can_manage_tenant(user.id, user.id)`, i.e. the caller's ownership of its own PERSONAL workspace, so a caller who owned one and was only NORMAL in the workspace it was acting in passed the guard on every provider, model-default and Langfuse write. Verified both ways against the running stack (member `108`, owner reaches the service). `TenantService.resolve_active_tenant_id` is NOT a fixed point on an already-resolved id - `test_resolve_active_tenant_id.py` pins the counterexample - so it must only ever be handed a user id.
- **Phase 2 Chat Session Dataset Binding**: A conversation carries its own dataset set. `conversation.kb_ids` is a nullable JSON column appended to the end of `migrate_db()` (NULL = inherit the assistant's `kb_ids`, `[]` = bound to nothing, two different answers). `apply_session_dataset_binding` decides a turn's set in one place and both turn entries plus the channel bootstrap call it: a session binding REPLACES the assistant's set, an explicit per-request `kb_ids` still unions onto the effective set, and a session bound to nothing does not fall back. `POST`/`PATCH` on `/chats/<id>/sessions` accept `dataset_ids` validated by the same read gate as the assistant path; reads publish `dataset_ids` with `null` for "inherits". On the web side the ONLY dataset UI is the existing chat-settings drawer (gear on the rail, gear in the header, and the header's dataset tags all open it): its dataset field edits the OPEN SESSION, an unchanged confirmation sends `null` so the session keeps inheriting, and the panel raises itself once per conversation when that conversation has no effective dataset - while a conversation that has one is left alone. When nothing is selected it states that in a notice and opens the model-and-datasets section; with a selection it keeps opening on retrieval. A URL-only placeholder session is created on save with the chosen binding, before the first question.
- **Dataset Files View - Open Frame**: The dataset files table no longer sits in a framed card: the `Card`'s own border is neutralised from the call site (`border-0 bg-transparent shadow-none`), `border border-table-border` is gone from the `Table` root class, the header/content gutters are `px-6 py-4`, and the `min-w-[880px]` width floor - which forced a page-level horizontal scrollbar on any viewport narrower than 900px - is removed. The table's remaining min-content floor came from the `run` column, whose `pipeline_name` is unbounded free text: the label now truncates inside a bounded cell and the tooltip beside it carries the full name. Verified in a real browser: the card and table compute to `border-width: 0` and the view has no page-level horizontal overflow. Measured residual, not a page-level problem: the `12rem` cap on the parse column means a long pipeline name makes the Table root's own `overflow-auto` scroll sideways by ~41px at a 1418px viewport (previously unbounded - the label had no cap at all, so it set the table's min-content width). A long name ellipsizes for real (`scrollWidth 579` vs `clientWidth 190`, `text-overflow: ellipsis`); the cap value is a one-line visual tradeoff that has not been tuned against a real long name on this stack.
- **Dark Theme - Slate Retune**: The `.dark` block in `web/tailwind.css` (the only place theme values live; not `src/`) was retuned from near-black surfaces to one matte Slate blue-grey ramp: page base `#1c1f26` (L 13%) -> `#23272f` (L 16%), card/popover `#232730` -> `#2a2e37`, input/raised `#282c37` -> `#31363f`, border `#2f3440` -> `#3c424e`, primary text `#c5cbd3` -> `#d0d4dc`, secondary `#818995` -> `#98a0ae`. The `--table-row-*` tokens were lifted too - the old `#1b2220` was DARKER than the page base. Surface steps stay 3 lightness points apart, so a card on a page and an input on a card remain distinguishable; text contrast lands at 10.1:1 / 5.7:1 rather than pure white's 15:1. The green accent family and the header colour are untouched.
- **Dark Canvas Token**: `--bg-canvas` and `--bg-list` were declared in `.dark` as `hsl()` triples while every consumer feeds them to `rgb()` (`rgb(var(--bg-canvas) / <alpha-value>)` in `tailwind.config.js`, `rgb(var(--bg-canvas))` in `components/canvas/background.tsx`). The browser does not reject that - it resolves the two spellings channel-wise, so the dark agent workflow canvas painted `rgb(220, 36, 41)`, a saturated red, in the middle of a matte Slate theme. Both are now RGB channel triples matching their hex siblings (`35 39 47` = `--bg-base`, `42 46 55` = `--bg-card`), which is what light mode always did. All ten `rgb(var(--x))`-consumed tokens were audited in both blocks; these two were the only mismatch. Verified in a real browser on `/agent/<id>`: the flow `<svg>` computes `rgb(35, 39, 47)`.
- **Team Role Label**: `setting.role` read "状态"/"State" while driving three role labels - the invite dialog's field and both roster headers - so the label contradicted its own control (choices member/admin, hint "角色即时生效"). Corrected to "角色"/"Role" for all three at once, verified in a real browser.
- **Invite Submit Hardening**: The invite path can no longer produce an unhandled rejection or a request that cannot succeed. `useAddUser`'s submit handler catches a rejected mutation (the request layer has already toasted it) instead of letting it escape the async callback; `useAddTenantUser` requires the active workspace id before building the request (no `/tenants/undefined/users`), reads the reply as `data?.code` (a body-less response used to throw INSIDE the mutation), and declining an invitation no longer fires with an undefined caller id. Covered by `use-add-tenant-user-guards.test.tsx` (4) and `use-add-user.test.tsx` (3).
- **Browser Verification Method**: Dialog/popup interactions are now verified in real Chrome driven over CDP (`Input.dispatchMouseEvent` / `Input.insertText` / `Runtime.exceptionThrown` capture), because jsdom cannot model hit-testing, stacking or pointer-outside dismissal and therefore PASSES interactions a browser fails. Two user-reported team-page defects (a select closing the invite dialog, and a crash on submit) did not reproduce under this method: both pickers render, apply and leave the dialog open, and no submit path throws.

### Pending / Active Implementation
- **Chat/Search/Agent isolation - decisions still open (recorded so they are not re-discovered as bugs)**: (1) one pre-existing orphan assistant (`Test`, `tenant_id` = the member's user id) is owned and listed correctly after the `created_by` backfill, but a turn on it still cannot resolve a chat model because its tenant does not exist - deleting it or rebinding it once to the workspace is a DATA decision, not a code one; (2) `update_message_feedback` hands `current_user.id` to `ChunkFeedbackService.apply_feedback`, which builds the doc-store index name from it; the correct subject there is the DATASET's workspace, a different answer from the assistant's workspace, so it needs its own change; (3) a chat assistant carries no rerank model by default (`dialog.rerank_id` is `""` unless an operator picks one, and `create` sets exactly that), so module C's reranker is absent on the chat path and the cut policy alone keeps a composite answer covered - wiring the tenant's default reranker into the chat path (as the search app does via `cable_defaults.search_config`) is a quality decision with a provider round trip per turn; (4) the `dialog.created_by` backfill attributes rows created during the short window when assistants were bound to the workspace (before `created_by` existed) to the WORKSPACE OWNER, because that window left no per-row record of the author - the live stack had no such row created by a member, but a deployment that ran that build should check for member-created assistants whose `created_by` it now wants reassigned.
- **Unreproduced team-page reports (needs the reporter's input)**: (1) "clicking the role/department select closes the invite dialog" - three real-browser passes show the dialog surviving, including the disabled-department + tooltip path; the next datum needed is whether the popup appears at all ("never appears" = stacking relative to the modal overlay, "appears then the dialog goes" = the dialog treating a popup portalled to `document.body` as an outside interaction, which this Radix Select does not register a nested-layer branch for); (2) "crash on submit" - no uncaught exception on three submit paths. Explicit shared-UI approval has now been granted and the Modal outside-interaction guard is implemented (see Completed Milestones); the Owner browser check still needs an authenticated session. The submit report remains unreproduced.
- **Jest baseline is not green (pre-existing)**: a full `npx jest` on this checkout reports 14 failed suites / 16 failed tests. Four of the suites are the parse error below; the rest are unrelated debt, neither introduced nor diagnosed here - `file-util` calls `Blob.arrayBuffer`, which jsdom does not implement, and `llm-util`'s tenant rerank expectation ("clears a stale tenant rerank model id when rerank is explicitly cleared") is stale. Do not read "0 failed" from a targeted run as the whole suite passing. `web/src/__tests__/theme-tokens.test.ts` passes.
- **Jest `.ts`-as-TSX parse failure**: `jest-esbuild-transformer.cjs` loads every `.ts` file with the `tsx` loader, so a `.ts` file containing a generic arrow type parameter (`src/pages/dataset/dataset/utils.ts:193`) fails to parse and takes 4-5 suites down with it (`dataset/dataset/{utils,hooks,parsing-status-cell}.test.ts`, `dataset/components/metedata/.../use-manage-values-modal.test.ts`, `hooks/__tests__/logic-hooks-use-scroll-to-bottom.test.tsx`). Pre-existing; the fix is to load `.ts` as `ts` and keep `tsx` for `.tsx`, after checking no `.ts` file in the tree relies on JSX.
- **Python test-harness environment (pre-existing)**: `test/unit_test/rag/advanced_rag` cannot be collected as a DIRECTORY on Windows - pytest's prepend import mode makes 11 of its modules import `rag.advanced_rag.*` against the wrong package object (`'rag.advanced_rag' is not a package`), and a pristine `HEAD` worktree reproduces it exactly, so it is not a regression to chase. Run those files individually; `test/unit_test/rag/nlp` (`python -m pytest test/unit_test/rag/nlp`) collects as a directory normally. The repo `.venv` also ships without `pytest-asyncio` even though `pyproject.toml` declares it, so async tests cannot even be collected until `uv pip install --python .venv/Scripts/python.exe "pytest-asyncio>=1.3.0"` is run - without it pytest dies with `INTERNALERROR: Unknown config option: asyncio_default_fixture_loop_scope`.
- **P3-01a/b**: Member removal asset transfer & cleanup logic (reassigning orphan private datasets upon workspace member removal).
- **P2-01~07**: Tenant invitation & onboard workflow for non-registered email addresses.
- **P4-04**: Owner-Tenant model configuration resolution for Assistant execution.
- **Test-harness** rot (`test/testcases/, p2): `test_http_api/test_session_management/test_session_sdk_routes_unit.py` (39 tests) cannot import: its `common.constants` / `user_service` / `joint_services.tenant_model_service` stubs predate names the real modules import (`PipelineTaskType`, `TenantService`, `get_first_tenant_model_name_by_type`, ...). It is `p2`, so a default run executes it. Those suites DO run on a dev host once `sdk/python` is on `PYTHONPATH` with a `ragflow_sdk-<version>.dist-info` on the same path and `ZHIPU_AI_API_KEY`/`SILICONFLOW_API_KEY` set (dummy values are enough), and `--level p3` selects the p3-marked ones. `test/testcases/test_http_api/test_file_management_within_dataset/test_doc_sdk_routes_unit.py` and `test/testcases/test_web_api/test_chunk_app/test_chunk_routes_unit.py` carry the same class of rot.

## Database Migration Rules (Peewee)
- **No Auto Schema Migration**: Peewee's `init_database_tables` skips existing tables without adding missing columns.
- **Adding Columns**: Any new model column MUST be appended via `alter_db_add_column(...)` at the end of `migrate_db()` in `api/db/db_models.py`.
- **Append-Only Migration**: Migration execution is strict append-only. NEVER delete, reorder, or modify existing `alter_db_*` calls.
- **Explicit Defaults & Nullability**: New columns MUST use explicit, conservative defaults or allow `NULL` (e.g. `department_id` nullable) so unassigned users/records are never incorrectly matched by authorization logic.

## Resource Ownership & Dataset Authorization (P3-03)
- **Decoupled Read vs Write Guards**: `can_read_dataset` (listing & vector retrieval) MUST be strictly decoupled from `can_write_dataset` (upload, parse, edit, delete). Users granted custom read/retrieval access MUST NOT inherit write or delete authority.
- **SQL-Level Dataset Masking**: Unpermitted datasets MUST be filtered out at the SQL query level in listing APIs. Never rely on frontend component hiding to conceal sensitive knowledge base metadata.
- **Dynamic Department Inheritance**: KB authorizations bound to `department_id` are dynamic—members joining or transferring into a department automatically inherit its assigned dataset retrieval access.
- **Dataset-File Hierarchy Consistency**: When a dataset is set to hidden/unpermitted, all underlying documents MUST inherit this suppression from the user's perspective during retrieval and UI views.
- **UX Tooltip Cleanliness**: Operational tooltips MUST be user-facing and plain language. Never expose implementation details like `(退出检索)` in tooltips or badges.

## Security & API Key Management
- **API Key Redaction**: API endpoints returning model instances MUST redact/mask `api_key` before output.
- **No Query Key Leaks**: Never pass or log raw credentials in GET query parameters.
- **Crypto Header Compatibility**: When encrypting stored credentials, preserve the `ENCRYPTED_MAGIC` (`b"RAGF"`) header bypass so unencrypted legacy database rows remain readable.
- **Registration Control**: Registration is strictly toggled via `REGISTER_ENABLED` in Python configuration.

## Resource Ownership & Assistant Scope
- **Asset Transfer Safety**: Member removal MUST explicitly transfer or reassign assets—never default private assets to shared visibility.
- **Assistant Model Resolution**: When a NORMAL user invokes a Tenant Assistant:
  1. Determine the effective tenant owning the Assistant.
  2. Resolve model/provider configuration using the **Owner Tenant** scope via `resolve_config_tenant_id`.
  3. Strict isolation: Normal users MUST NOT use this resolution path to access model configurations of unrelated tenants.

## Code Layout
- `api/`: Active Python API server, blueprints, services, and database layers.
- `rag/`: Core ingestion, retrieval, LLM integration, and graph RAG logic.
- `deepdoc/`: Document parsing and OCR pipeline.
- `agent/`: Workflow canvas, components, tools, and execution engine.
- `internal/`: Go application code (Legacy/Inactive for current tenant refactor).
- `web/`: React + TypeScript + Vite frontend. Follow `web/CLAUDE.md` for web-specific conventions.
- `docker/`: Compose files for dev and production deployments.

## Working Rules

Before editing:
1. Identify the owning code path in `api/` or `web/`.
2. Trace request flow: Route → Service → Database/Resource.
3. Verify **Active Tenant Context** (`active_tenant_id`) and **Resource Owner**.
4. Check explicit RBAC permission requirements (`OWNER`, `ADMIN`, or `NORMAL`).
5. Keep modifications local, surgical, and minimal. Do NOT modify Go files for symmetry.

After editing:
1. Inspect git diff for unintended changes.
2. Run targeted tests (Python `pytest` / `ruff`, Frontend `npm run lint` / `type-check`).
3. Verify both **ALLOWED** and **DENIED** authorization outcomes for permission edits.